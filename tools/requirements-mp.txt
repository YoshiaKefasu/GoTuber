# Phase 2 用 MediaPipe サイドカー依存
# pip install -r tools/requirements-mp.txt
# セットアップ手順: docs/PHASE2.md 4.4 参照
# 注意: Phase 1 (Go バイナリ) の動作には不要。Phase 2 で初めて使う。
# Phase 2.10: Python→Go 通信は localhost TCP JSONL に移行。
# MediaPipe Face Landmarker モデルは assets/models/face_landmarker.task に同梱済み。
# 通常はネットワーク不要。モデルの配布元は Google / MediaPipe、Apache-2.0。

mediapipe>=0.10.14,<0.11
opencv-python>=4.10.0,<5
numpy>=1.26.0,<2.0
